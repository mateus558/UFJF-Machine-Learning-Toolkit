dist: bionic
group: travis_latest

os: linux
language: python

matrix:
  include:
    - env: CXX=gcc
    - env: CC=clang

install:
  - pip3 install meson ninja

script:
  - chmod +x build.sh
  - bash build.sh